bandit[toml]
coverage
hypothesis
mypy
mutmut
pip-audit
pytest
pytest-cov
pytest-timeout
pytest-xdist
radon
ruff
tox
vulture
xenon

pip-tools

# libcst selects this free-threaded YAML build on Python 3.13+; pin it explicitly
# so one protected hash lock installs on every supported Python version.
pyyaml-ft==8.0.0; python_version >= "3.13"
uv==0.12.0
